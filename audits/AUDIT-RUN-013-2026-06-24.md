# AUDIT-RUN-013 — v10.1: OS-keyring backend for api-key-manager

## 1. Header
- **Run:** 013 · **Date:** 2026-06-24 · **Agent:** Claude (Cowork)
- **Scope:** v10.1 delta — `api-key-manager` gains an **OS-keyring** backend (Keychain / Credential Manager / Secret Service via `keyring`), with the git-ignored `600` file as fallback; master §10.4 + `ralph/multi-vendor-agents.md` updated. Security-focused review.
- **Depth / assurance:** Deep; Reasonable.

## 2. Findings — none new

### Security re-review (the change is secret-handling) — **PASS**
- **More secure by default:** keyring stores secrets **encrypted at rest** in the OS store; `auto` prefers it. The repo keeps only a **non-secret index of key *names*** for keyring (`keyring-index.json`) — verified to hold names, never values.
- **Fallback unchanged-safe:** file store remains `600` / dir `700` / outside the repo; `.gitignore` blocks `keys.env` / `*.env` / `*.key`.
- **No leakage:** values read from env / hidden stdin (never argv); masked in `list`/`get`; `--reveal` warns; `export-env` is meant for `eval "$(…)"` so the agent's visible output never shows secrets.
- **Tested:** backend resolution (`auto`→file in headless; `--backend keyring`→graceful "install keyring" error, no crash); file CRUD + `export-env` all work; **repo secret-scan = 0 hits**.

### Carried observations (unchanged)
- F012-1 (S5): adapter stage-shorthand "A→E". Left as-is.

### Verified clean
- Integrity: all files end at their intended last lines (bash/shipping-path writes); master renamed `…-v10.1.md`; **0 stray v10.0 refs**; CI `validate` 13/13; sections 0–10, stages A–E+R, principles 1–9, 42 modules intact.

## 3. Audit of the Audit
- Verified against git after commit (RUN-010 control). The keyring code path was exercised in a headless environment to confirm it degrades gracefully rather than crashing — important, since most local/dev machines DO have a keyring but CI/headless ones don't.

## 4. Exit
**Complete — v10.1 PASSES.** Keys can now be stored and retrieved via the OS keyring (preferred, encrypted) or the git-ignored file fallback, loaded into sub-agent environments without exposing values; no secrets in the repo.
